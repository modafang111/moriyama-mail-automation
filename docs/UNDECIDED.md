# 未確定事項

決まっていない項目だけ残しています。推測で埋めていません。

## いま確定したこと

- 除外: MyASPで、その配信のときだけ送らない。リストから外す処理ではない
- 依頼受付: 専用フォーム
- 専用フォームの通知先（1件目）: modafang111@gmail.com（モダ）
- MyASPはプランが2つあり、依頼時に選択する
- 本番の即時配信は、今は不要（予約配信）

## まだ決めていないこと

### 「削除」

前回は MyASP の用語（ユーザー削除 / 配信解除）で聞いてしまい、分かりにくくしました。

元の依頼文には「配信対象への追加」「配信対象からの削除」「今回の配信からの除外」とありました。除外は上のとおり確定です。

「削除」が別操作として必要かどうかは、まだ決めていません。画面からは外してあります。

必要なら、次のどれかだけ教えてください。

- 次回以降も送らない（リストから外す）
- それ以外の操作
- 削除という操作は使わない

### 依頼フォーム

- 社外の顧客が直接開く Web フォームにするか、このWindows画面の専用フォームで担当者が入力するか
- 項目の正式な並び、必須項目
- 添付の受け取り方

### 配信対象データ

- CSVの列名、文字コード
- 追加と除外を別ファイルにするか

### 通知・テスト

- 通知先の2件目のメールアドレス
- テスト配信先
- SMTP（Gmailアプリパスワードなど）

### MyASP / Google

- 2つのプランの正式名称とシナリオID
- ログインURL、認証
- DriveのフォルダID
